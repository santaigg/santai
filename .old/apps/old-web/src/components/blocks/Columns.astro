---
import { cn } from "../../utils/cn";

interface Props {
	class?: string;
	style?: string;
}

const { class: className, style } = Astro.props;
---

<div class={cn("flex flex-col md:flex-row ", className)} style={style}>
  <slot />
</div>
