{
  "name": "santai",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "dev": "turbo run dev",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "check-types": "turbo run check-types",
    "create": "node scripts/create-workspace.js",
    "clean-modules": "node scripts/clean-node-modules.js"
  },
  "devDependencies": {
    "prettier": "^3.5.0",
    "turbo": "^2.4.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.2.3",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@types/tmi.js": "^1.8.6"
  }
}
