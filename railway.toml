[build]
builder = "NIXPACKS"
buildCommand = "git clone --recursive $RAILWAY_GIT_REPO_URL . && bun install && bun run build"

[deploy]
startCommand = "bun run start"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10 